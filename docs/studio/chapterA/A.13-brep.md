# A.13 BREP

BREP files are used to import CAD geometry. The BREP file reader is supported mostly through a third-party library, called OpenCascade and should be able to read a wide variety of BREP files.

The BREP file reader will import the entire geometry as a single object.
